# Plan - Performance Optimization Phase 1 - COMPLETED

## Мета
Реалізація "Quick Wins" для прискорення роботи програми при великій кількості тікетів.

## Зміни
1.  **Ticket Indexing (`HashMap`)**:
    *   Файл: `src/model/board.rs`
    *   Зміна: Додати `HashMap<String, Ticket>` (або `Arc<Ticket>` якщо потрібно шарити) до структури `Board`.
    *   Зміна: `Board::load` та `Board::load_all_queues` повинні наповнювати цей індекс.
    *   Зміна: Оптимізувати `find_ticket_by_id` для використання цього індексу.

2.  **Log Loading Optimization**:
    *   Файл: `src/model/stats.rs`
    *   Зміна: Переписати `get_board_summary`, щоб `load_all_logs` викликався один раз.
    *   Зміна: Передати зріз логів `&[LogEntry]` у всі функції розрахунку метрик.

3.  **Search Debounce**:
    *   Файл: `src/main.rs` або `ui/app.slint`
    *   Зміна: Додати затримку перед викликом `controller.reload()` при зміні тексту пошуку.

## Тести
1.  **Unit Tests**:
    *   Перевірити, що `find_ticket_by_id` повертає правильні тікети після завантаження.
    *   Перевірити коректність розрахунку статистики з новими сигнатурами функцій.
2.  **Integration Tests**:
    *   Перевірити швидкість завантаження великої кількості тікетів (опціонально, як бенчмарк).

## Ручна перевірка
1.  Запустити програму з 100+ тікетами.
2.  Швидко перемикати фільтри та вводити текст у пошук.
3.  Відкрити вікно статистики — воно має відкриватися швидше ніж раніше.
