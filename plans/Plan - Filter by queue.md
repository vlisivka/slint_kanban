# Plan - Filter by queue

## Meta
- **ПОТОЧНЕ_ЗАВДАННЯ**: Filter by queue (Search and Filter section).

## Мета
Додати можливість вибирати, які колонки (черги) відображати на дошці. Це допоможе користувачам фокусуватися на конкретних етапах роботи, якщо черг забагато.

## Зміни

### [Component Name] UI

#### [MODIFY] [app.slint](file:///home/vlisivka/workspace/slint_kanban/ui/app.slint)
- Додати `visible: bool` до структури `QueueStr`.
- В `App` додати властивість `show_filter_menu: bool`.
- Додати кнопку "Filter Queues" поруч із рядком пошуку.
- Додати випадаюче меню або overlay зі списком чекбоксів для кожної черги.
- В основному циклі `for queue in board_queues` додати перевірку `if queue.visible : KanbanColumn { ... }`.

## Рев'ю та покращення дизайну (за фідбеком користувача)
- **Розміщення**: Перенести кнопку фільтра у правий верхній кут.
- **Іконка**: Замінити текст "Filter Queues" на іконку (символічну).
- **Діалог**:
    - Зменшити вертикальні відступи та порожній простір.
    - Зробити кнопку "Close" стандартного розміру та вигляду.
    - Використати `preferred-height` для діалогу, щоб він не розтягувався по вертикалі.
    - Видалити `alignment: start` з основного `VerticalLayout` діалогу, щоб він щільно облягав вміст.

### [Component Name] Logic

#### [MODIFY] [main.rs](file:///home/vlisivka/workspace/slint_kanban/src/main.rs)
- Додати механізм збереження стану видимості черг в `Config`.
- Оновити `sync_ui_with_board`, щоб він заповнював поле `visible` для кожної черги.
- Реалізувати callback для перемикання видимості черги.

## Тести
### Автоматичні тести
- Додати тест в `main_tests.rs`, який перевіряє, що при встановленні `visible: false` для черги, вона не відображається в моделі (або хоча б властивість встановлена правильно).

## Ручна перевірка
1. Запустити додаток.
2. Натиснути кнопку "Filter Queues".
3. Зняти галочку з однієї з черг (наприклад, "Done").
4. Переконатися, що колонка "Done" зникла з дошки.
5. Натиснути знову і повернути її.
