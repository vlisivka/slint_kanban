---
author: user
created_at: 2026-07-05 21:38:22
updated_at: 2026-07-05 21:38:22
---
# План реалізації

## Зміни

### 1. `ui/dialogs/ticket_view.slint`
- Прибрати callback-и `show-tooltip(string, length, length)` і `hide-tooltip()`
- Додати вхідний `callback navigate-to(string)` (вже є)
- Замінити `TouchArea { changed has-hover => { show-tooltip(...) / hide-tooltip() } }` на:
  ```slint
  Rectangle {
      Tooltip { text: @markdown(ref.title); }
  }
  ```
- Це в 2 місцях: посилання у секції References та в коментарях

### 2. `ui/app.slint`
- Прибрати state variables (lines ~78-82): `tooltip_text`, `tooltip_visible`, `tooltip_x`, `tooltip_y`
- Прибрати callback-и (lines ~443-452): `show-tooltip(text, x, y)`, `hide-tooltip()`
- Прибрати floating Tooltip Rectangle (lines ~572-591)

### 3. Перевірка що все компілюється і тести проходять

